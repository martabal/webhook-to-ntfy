services:

  - name: "grafana" 
    config:
        icon: "desktop_computer" #Optional
        topic: "server"
        priority: 3 #Optional

  - name: "gitea"
    config: 
      icon: "desktop_computer" #Optional
      topic: "server" #Optional
      priority: 3 #Optional
    
  - name: "overseerr"
    config: 
      icon: "desktop_computer" #Optional
      topic: "server"
      message: "Message" #Optional
      title: "Title" #Optional
      priority: 3 #Optional